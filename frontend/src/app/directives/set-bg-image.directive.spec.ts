import { SetBgImageDirective } from '../set-bg-image.directive';

describe('SetBgImageDirective', () => {
  it('should create an instance', () => {
    const directive = new SetBgImageDirective();
    expect(directive).toBeTruthy();
  });
});
