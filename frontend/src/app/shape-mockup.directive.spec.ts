import { ShapeMockupDirective } from './shape-mockup.directive';

describe('ShapeMockupDirective', () => {
  it('should create an instance', () => {
    const directive = new ShapeMockupDirective();
    expect(directive).toBeTruthy();
  });
});
